"""
Metrics aggregation (Phase 5/6).
"""


def compute_metrics(packet_log) -> dict:
    """
    TODO: from a log of per-packet outcomes, compute:
      - delivery ratio
      - average end-to-end latency
      - average hop count
      - fraction misrouted due to stale routes
    """
    raise NotImplementedError
